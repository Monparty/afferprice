# คัดลอกเป็น .env แล้วเติมค่า (docker-compose.yml อ่าน .env อัตโนมัติ)
#   cp .env.example .env

# service_role key — จาก `npx supabase status` (local demo key มาตรฐาน เหมือนกันทุกเครื่อง)
SUPABASE_SERVICE_ROLE_KEY=

# Omise secret key (skey_test_...) — จาก Omise Dashboard
OMISE_SECRET_KEY=
